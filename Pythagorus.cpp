#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    double n,m;
    cin >> n >> m;
    cout << fixed << setprecision(6) << sqrt((n*n) + (m*m));

    
    return 0;
}
